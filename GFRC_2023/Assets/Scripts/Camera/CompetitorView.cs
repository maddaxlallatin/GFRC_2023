using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CompetitorView : MonoBehaviour
{

    // public float CompTarget;
    // public Vector3 Path;

    // void CompTargeting()
    // {
    // if (Path.x < 0 && Path.z > 0)
    // {
    // CompTarget = 180-((180/Mathf.PI)Mathf.Atan(Path.z/Path.x)+360);
    // }
    // else if (Path.x > 0)
    // {
    // CompTarget = 180-((180/Mathf.PI)Mathf.Atan(Path.z/Path.x)+180);
    // }
    // else 
    // {
    // CompTarget = 180-(180/Mathf.PI)*Mathf.Atan(Path.z/Path.x);
    // }
    // if (CompTarget < 0)
    // {
    // CompTarget = CompTarget + 360;
    // }
    // }
}
